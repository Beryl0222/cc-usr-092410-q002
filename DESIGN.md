# 沙箱治理设计

本文件说明教学平台主管要求的六项分账与各条治理规则如何落到 `domain.py`，
便于审计与后续接口化。所有规则都有对应的契约测试（`domain_contract.py`）。

## 1. 六账分离

六类业务记录各自独立建账，互不混存，彼此只通过**不可变版本引用**绑定：

| 分账 | 存储 | 说明 |
| --- | --- | --- |
| 课程案例 | `Sandbox.cases` | 教学目标字段、病例与课程的多对多关系 |
| 临床数据快照 | `Sandbox.snapshots` | 键为 `(病例, 版本)`，构成不可变身份；五要素全等才可幂等重放，异内容重传记入 `rejected_imports`，勘误只发新版本 |
| 数据使用同意 | `Sandbox.consents` | 可限定课程；可撤回，撤回即时生效 |
| 脱敏策略 | `Sandbox.policies` | 字段变换（丢弃/泛化）与 k 匿名阈值 |
| 分析环境 | `Sandbox.environment_versions` | 键为 `(镜像, 版本)`，含工具清单与摘要 |
| 作业产物 | `Sandbox.assignments` | 结论、处理步骤、评分时冻结的环境指纹、复核记录 |

限时切片 `sessions`、披露决策 `exports`、注册 `enrollments`、
任课关系 `teacher_courses`、审计 `audit`、被拒绝导入 `rejected_imports`
是运行记录，不并入六账。`rejected_imports` 只追加，登记所有被拒绝的快照
导入（身份冲突 / 记录损坏 / 内容摘要不符 / 非法替代关系），永不回流快照账。

病例账不含数据行，数据账不含作业结论——分账边界由 `SeedTest` 固定。

## 2. 限时、目标匹配、脱敏的数据切片

- `create_task` 在任务创建时**钉版**：当时最新的快照版本与环境版本写入任务，
  之后的勘误、升级不改变既有任务（前向影响原则）。
- `issue_slice` 依次校验：注册关系 → 课程窗口 → 有效同意，
  然后 `_project_rows` 只投影教学目标字段：
  - 身份字段（`identity_fields`，如 patient_id/姓名/电话）一律剔除；
  - 策略变换执行（如年龄按十岁段泛化、城市字段 drop）。
- 切片带 `expires_at`（默认 240 分钟）；过期、撤回后不可用于提交或导出。

## 3. 导出前的披露检查

`request_export` 不直接放行，统一进 `_disclose`，五项检查任一不过即**阻断**：

1. 沙箱会话在有效期内、未撤回；
2. 课程窗口开放且学生注册未撤权；
3. 存在覆盖该病例+课程且未撤回的同意；
4. 导出列不含任何身份字段；
5. 每个导出分组的样本数 ≥ 策略 `k_threshold`（小样本阻断）。

每次决策与理由写 `audit`，并关联到作业（便于从结论反查导出）。

## 4. 同意撤回的即时效力

`withdraw_consent` 对**所有**引用该病例的任务生效（同意可跨课程，
故撤回也跨课程），与“勘误/升级只前向影响”刻意区分：

- 进行中的沙箱会话立即撤回；
- 队列中或曾获批的导出立即转为阻断并追加“同意撤回”理由；
- 受影响作业跨课程列清（`report["assignments"]`），区分
  “已评分：保留冻结指纹并标记风险”与“未评分：会话撤回且导出阻断”；
- 已评分作业**不删除、不改写**，只追加“同意撤回”风险标记；
- 禁止重复撤回。

撤回后新的小样本导出会同时命中“小样本”和“同意撤回/会话撤回”，双重阻断。

## 5. 勘误与工具升级：只影响新任务

- 快照勘误用 `add_snapshot(..., replaces=n)` 发布 v(n+1)，旧版行数据原样保留；
- 工具升级用 `add_environment` 发布同镜像新版本。
- 二者都**不回溯**：旧任务仍指向钉住的版本，新任务创建时才取最新版。
- 对评分之后才发生的变化，已评分作业通过 `risk_flags` 标出
  （事件路径：`add_snapshot`/`add_environment`；装载路径：`_reconcile_flags`，
  保证从夹具重建后风险标记一致、可重复）。
- 勘误标记沿**整条替代链**补发：v3 发布时，凡冻结在链上任一旧版本（v1、v2）
  的已评分作业都会收到指向 v3 的标记（`_flag_snapshot_chain`），
  不因中间隔了一版而漏标。

## 5b. 快照不可变身份与冲突拒绝

`(病例编号, 版本号)` 一经首次提交即构成不可变身份。`add_snapshot` 对同一身份
的重传逐字段比对五要素：

| 要素 | 取值 |
| --- | --- |
| 规范化内容 | `digest(rows)`，经 `sort_keys` 规范化，键序不同不算差异 |
| 身份字段 | `identity_fields` 列表 |
| 发布时间 | `released_at` |
| 替代关系 | `replaces` |
| 勘误说明 | `erratum_note` |

- 五者完全一致：**幂等重放**，返回原记录，不新增任何账内数据、不重复打标记；
- 任一不同：抛 `SnapshotConflict`，并在 `rejected_imports` 与 `audit` 登记
  “身份冲突”（含提交载荷的重算摘要，不含 rows 本体）。原快照、任务钉版、
  已签发切片、导出决策、已评分作业及其冻结指纹**全部保持不变**，
  `reproduce_report` 仍判“复现一致”。

登记临界区由 `_snapshot_lock` 保护（`RLock`），check-then-insert 原子完成：
并发导入同一版本时只有一个线程能成为首次提交者；与其载荷相同者幂等重放，
相异者全部收到冲突。

**替代链合法性**（`_validate_replacement_chain`）在首次入库前强制：

- 首个版本必须是 v1 且 `replaces` 为空；之后版本必须声明 `replaces`；
- `replaces` 必须指向已存在的 v(n-1)：乱序、跳版、重复替代（链分叉）拒绝；
- `replaces >= version` 或沿链回溯成环（循环替代）拒绝；
- 新版本发布时间不得早于被替代版本（时间乱序拒绝）。

## 5c. 重启恢复与追溯视图

`replay_snapshot_journal(path)` 支持 JSONL、快照信封 JSON、裸数组与单条对象，
逐条走与在线导入**完全相同**的 `_register_snapshot` 校验：

- 行不可解析 / 必填字段缺失 / 时间非法 / 未知病例 → “记录损坏”；
- 自带 `content_hash` 与规范化内容重算不符 → “内容摘要不符”（疑似损坏或篡改）；
- 乱序、跳版等 → “非法替代关系”；同版本异内容 → “身份冲突”。

坏记录只追加到 `rejected_imports`（带来源文件与行号），不中断后续记录、
不污染任何分账；合法记录正常参与钉版、投影与沿链标记。整份日志重复恢复
幂等（首轮 accepted，之后 replayed）。

`snapshot_lineage(case_id)` 给出替代链与当前有效版本（链头取最大版本号）；
`trace()` 的“版本谱系”段同时呈现：作业冻结的版本与内容摘要、
冻结版是否仍是当前版、当前有效版本、完整替代链、针对该病例的全部被拒绝导入，
使教师能同时看到“当时依据什么、现在有效什么、哪些重传被拒”。

## 6. 环境指纹与教师复现

- `grade_assignment`（仅任课教师）在评分瞬间调用 `environment_fingerprint`
  冻结四要素：快照版本+内容摘要、脱敏策略版本+摘要、环境镜像版本+摘要、
  处理步骤（recipe）及其摘要。
- 评分只能一次：重复评分抛错，复核记录只追加。
- `reproduce_report` 在冻结版本中重放 recipe：
  - 校验任课关系——教师看不到非任课课程（即使病例相同）；
  - 校验每个步骤的工具存在于冻结镜像（外部工具/升级版工具导致复现不一致）；
  - 重算结果摘要与评分时 `result_hash` 比对；
  - 报告显式给出 `identity_access: 拒绝`——教师角色没有
    `patient_identity:read` 能力，且沙箱从不下发身份字段（两道闸）。

## 7. 跨校到期自动收权

`sweep_expired(now)` 对课程窗口结束的注册关系置“已撤权”，
并撤回该学生该课程进行中的会话；跨校课程无宽限，其他课程不受影响。
该操作幂等，且在每次取切片时也会执行，避免依赖定时任务才能生效。

## 8. 结论溯源

`trace(assignment_id)` 对任一结论输出四段：

- **数据范围**：病例、快照版本与内容摘要、教学目标字段、切片编号；
- **处理步骤**：脱敏策略版本、环境镜像版本与摘要、recipe 与摘要；
- **课程授权**：课程、窗口、跨校标记、学生注册状态、各同意记录及撤回时间；
- **教师复核**：评分/复核留痕（教师、时间、等级、意见）。

另附风险标记与冻结指纹。`assignments_for_case` 提供病例维度的影响面清单
（撤回、勘误时清点两门课程的全部作业）。

## 9. 边界与后续

当前实现是内存领域模型 + 种子夹具，用来固定规则语义；HTTP 化时建议：
鉴权中间件强制角色能力、所有写操作落审计账、`sweep_expired` 由定时任务与
请求路径双触发。快照行数据在生产中应存对象存储，账内只保留版本与摘要。
